interface:
  display_name: "Team Plan"
  short_description: "Run Plan for one artifact directory"
  default_prompt: "Use $team-plan to run Plan for one artifact directory."

policy:
  allow_implicit_invocation: false
