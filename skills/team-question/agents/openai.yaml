interface:
  display_name: "Team Question"
  short_description: "Run Question for one artifact directory"
  default_prompt: "Use $team-question to run Question for one artifact directory."

policy:
  allow_implicit_invocation: false
