interface:
  display_name: "Team Research"
  short_description: "Run Research for one artifact directory"
  default_prompt: "Use $team-research to run Research for one artifact directory."

policy:
  allow_implicit_invocation: false
