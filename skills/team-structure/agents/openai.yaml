interface:
  display_name: "Team Structure"
  short_description: "Run Structure for one artifact directory"
  default_prompt: "Use $team-structure to run Structure for one artifact directory."

policy:
  allow_implicit_invocation: false
