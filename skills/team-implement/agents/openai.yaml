interface:
  display_name: "Team Implement"
  short_description: "Run Implement for one artifact directory"
  default_prompt: "Use $team-implement to run Implement for one artifact directory."

policy:
  allow_implicit_invocation: false
