interface:
  display_name: "Team Design"
  short_description: "Run Design for one artifact directory"
  default_prompt: "Use $team-design to run Design for one artifact directory."

policy:
  allow_implicit_invocation: false
