interface:
  display_name: "Team PR"
  short_description: "Run PR for one artifact directory"
  default_prompt: "Use $team-pr to run PR for one artifact directory."

policy:
  allow_implicit_invocation: false
