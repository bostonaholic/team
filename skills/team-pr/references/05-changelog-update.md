## Changelog Update

Before creating the ship commit, read [changelog rules](changelog.md) and
update `CHANGELOG.md` per that reference. Filter to user-facing commits:
`feat:`, `fix:`, `perf:`, `security:`, and any `BREAKING CHANGE:` footer.
Exclude `chore:`, `test:`, `refactor:`, `ci:`, `docs:`.
