export const FALLBACK_CANDIDATE_MARKER = "SCOUT_RETURN_ACCEPTED_7A";
export const FALLBACK_CANDIDATE_PATH = "src/normalize-label.pseudo";
export const FALLBACK_CANDIDATE_FACT =
  'normalize_label replaces every space with "-" and preserves letter case.';
export const FALLBACK_CANDIDATE =
  `${FALLBACK_CANDIDATE_MARKER} ${FALLBACK_CANDIDATE_PATH}:1 ${FALLBACK_CANDIDATE_FACT}`;
