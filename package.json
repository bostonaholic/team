{
  "name": "team",
  "version": "0.13.0",
  "description": "Drive a feature from idea to PR with a team of Claude Code agents.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "test": "bun test",
    "test:periodic": "EVALS_ALL=1 EVALS_TIER=periodic bun test --concurrent --max-concurrency 15 ./tests/*.evals.ts",
    "test:gate-evals": "node scripts/run-gate-evals.mjs",
    "eval:select": "bun run scripts/eval-select.ts",
    "eval:list": "bun run scripts/eval-list.ts",
    "eval:compare": "bun run scripts/eval-compare.ts"
  },
  "keywords": [
    "automation",
    "cli",
    "claude",
    "ai-agent",
    "devtools"
  ],
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
