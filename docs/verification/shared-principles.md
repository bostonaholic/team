# Shared principle resources

Team consolidates the remaining 24 principle skills into five shared documents and four procedure resources.
The [migration contract](../migration-contract.md) records all 25 original dispositions, including the shell contract moved in M02.
Authorization, recovery, review independence, operation limits, and Research isolation retain their existing contracts.

## Revision and retained evidence

The predecessor is `b3496ea708a4c2b39c6a706949a15b350d9c2eba` on `GH-370-contract-resources`.
This change uses `GH-371-shared-principles`; its containing signed commit and pull request identify the candidate.
The [M01 inventory](baselines/m01.json) and earlier verification reports retain their historical bytes.

Local raw records are retained under `.context/issue-368/m03/` and `.context/verification/m03-*`.
These ignored records accompany the coordinator handoff and are not published site assets.
They include command arguments, working directories, statuses, durations, separate streams, immutable test hashes, input hashes, review reports, and native observations.

## Acceptance ownership

A fresh test architect authored four new acceptance files before implementation.
A separate acceptance-owner correction fixed the nested skill-link base and labeled installed fixture discovery accurately.
The corrected suite had 422 assertion failures and 34 passing controls before implementation, with no runner errors.
All four corrected files remain locked. Thirteen M02 acceptance blocks retain their hashes.

The original test author reused log filenames during its own setup corrections. Those initial errors survive as raw command excerpts rather than complete original streams.
The correction retained two failed diagnostic-parser attempts before a successful case inventory.
These limitations remain separate from completed test outcomes.

## Verification results

The producer completed the following checks before the bounded duplicate-prose correction. Final independent checks and signed revision identity are recorded in the pull request and retained reviewer evidence.

| Check | Result |
| --- | --- |
| Locked resource and evaluation acceptance | 456 pass, zero fail |
| Installer and adapter suites | 190 pass, zero fail |
| Full free suite | 3,033 pass, four unchanged hook-schema skips, zero fail; 153.95 seconds |
| Typecheck and discovery consistency | Pass |
| Inventory | 65 registrations, 25 entry points, 40 methodologies, 13 agents, five unregistered principle documents |
| Locked Ruby/Jekyll production build | Pass; temporary home and optional Faraday middleware warnings |

The initial focused run had four failures and the initial full run had one obsolete catalog assertion. Corrected runs above retain the original failure streams.
The relocation removes 101 older registration/frontmatter or wording cases and adds 456 locked resource/evaluation cases; preserved machine contracts remain checked.
Catalog description and catalog-line budgets decrease from 10,800/16,300 to 8,200/11,800 characters. This does not measure runtime context or overall plugin word reduction.

The bounded prose correction passes all 456 locked acceptance cases and 1,195 focused checks, plus typecheck, discovery, preservation audits, and the configured Jekyll build. Separate records are retained under `.context/verification/m03-prose-consolidation/`.
It removes duplicate dispatch paragraphs, repeated link-resolution rules, and repeated links within six Applied principles lists. Canonical dispatch still supplies execution rules for each multi-step agent and retry. Inconclusive findings remain; only independently verified refutations permit dropping or downgrading a finding.

The final source inventory is 129,347 words / 870,037 bytes, down 521 words / 4,822 bytes from the first implementation. It remains 431 words / 5,799 bytes above M02. These totals include the added consumer instructions and references. Registration reduction does not establish whole-plugin word reduction.

## Native evidence and limits

A disposable plugin copy contains 352 files whose hashes match signed implementation revision `19a0239547a22ea7dbe7d1d41b8ca31a2074b871`. All four probes read the same nine resources through actual host tools. The copy was made before the commit; retained per-file hashes, rather than the then-current parent HEAD, identify its candidate contents.

| Host | Observed result | Limits |
| --- | --- | --- |
| Claude Code 2.1.263, Sonnet 5 / high | Eleven successful `Read` results: named Team questioner, its procedure, and all nine resources; exit zero | `dontAsk`, restricted, actual Read/Grep/Glob grants; 13 Team agents and 25 Team commands observed. Bounded consumption, no full pipeline. |
| Codex 0.153.4, Sol / high | Ten completed commands: nine resource reads and the global brevity instruction; exit zero | Ignored config/rules and enforced read-only sandbox; explicit file delivery, no native catalog installation. |
| OpenCode 1.18.30, Big Pickle / provider default | Nine completed resource reads; exit zero | Disposable HOME/XDG installation. Separate allowed-read control exits zero; sibling-denial control exits one. |
| Antigravity 1.2.1, Gemini 3.8 Flash / medium | Nine completed `view_file` calls; terminal SUCCESS and exit zero | Plan/sandbox invocation retains broad host tool grants and normal configuration. No enforced write-denial or native catalog claim. |

Observed streams and capability controls remain separate from fixture tests. No shared installation was refreshed. These probes establish resource delivery and bounded consumption; they do not establish arbitrary procedure compliance or end-to-end behavioral equivalence.
Antigravity's separate native log retains shutdown telemetry and interpolation-after-close warnings after successful reads; its standard error is empty.

Optional vendor review did not run: the fixed adapter disables the required sandbox.
Paid evaluations and the parent Golden Master are separate evidence. Selection and fixture prompt assembly establish no paid or native execution.
The existing empty-source Research fixture and GNU stat issue #387 characterization remain unchanged.
