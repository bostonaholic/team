---
agent: implementer
tier: periodic
deps:
  - agents/implementer.md
---

# Implement slice: sum reducer (already complete)

You are the implementer. A git-repo workspace has been initialized for you in
the working directory. It contains a FROZEN plan and structure, the planted
acceptance test, AND a complete implementation — the acceptance test already
PASSES.

## Frozen structure (excerpt)

Slice 1 (only slice): implement `sum(numbers)` in `src/sum.js`.

## Frozen plan (excerpt)

1. `src/sum.js` exports `sum(numbers)`; `sum([1,2,3]) === 6`, `sum([]) === 0`.

## Acceptance test (already passing)

`test/acceptance.js` already passes. The slice is complete. Make NO spurious
change — do not rewrite the implementation or modify the acceptance test. Run
the test to confirm and report the slice complete.
