---
agent: skills/progress-tracking
tier: periodic
deps:
  - skills/progress-tracking/**
---

You are about to carry out this three-step release checklist:

1. Bump the version in `package.json`.
2. Update the `CHANGELOG.md` release heading.
3. Tag the release in git.

Before doing any of it, seed a todo ledger with one item per step, then narrate
your progress by showing the ledger after each step (mark each item done as you
go). Show the ledger as a checklist in your response — you do not need to touch
real files; demonstrate the tracking convention itself.
