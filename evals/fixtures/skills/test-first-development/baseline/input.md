---
agent: skills/test-first-development
tier: periodic
deps:
  - skills/test-first-development/**
---

Design an `isPalindrome(s)` helper using test-first development. In your
response, write the tests FIRST (as a fenced code snippet), state that they
fail before any implementation exists, and only then write the implementation
that makes them pass. Output everything as text in your answer — you do not
need to create real files or run a test runner.
