# Rubric: progress-tracking skill

1. Seeds one ledger item per numbered step before starting (per-step granularity, not per-phase or per-file).
2. Marks each ledger item complete as its step lands, leaving a per-step trail rather than a single lumped status line.
